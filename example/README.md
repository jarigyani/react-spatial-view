# React Spatial View Example

Minimal Vite + React demo for this repository.

```bash
cd example
npm install
npm run dev
```

The example resolves `@jarigyani/react-spatial-view` to `../src/index.ts`, so it runs against the local source without publishing or building the package first.
